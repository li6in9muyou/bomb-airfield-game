namespace GameLogic;

public class GameLogic
{
    public bool setAirplane(int x, int y, string direction)
    {
        //传入飞机头，方向，经过算法(排除机头坐标重复，飞机重叠，坐标超载等情况)，返回bool值，true成功--》传入数据类中，false失败--》需要再次输入(逻辑在界面类实现)
        var flag = false;


        return flag;
    }
}